name: Update Scores

on:
  schedule:
    # Every 5 minutes (GitHub's minimum) during the tournament
    # (Jun 11 - Jul 19 2026); GitHub may delay scheduled runs by a few
    # minutes under load. Runs with unchanged scores skip commit and deploy.
    - cron: '*/5 * * * *'
  workflow_dispatch:

concurrency:
  cancel-in-progress: false
  group: update-scores

permissions:
  contents: write

jobs:
  update:
    outputs:
      changed: ${{ steps.commit.outputs.changed }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          cache: npm
          node-version: 22
      - run: npm ci
      - env:
          API_URL: ${{ vars.API_URL || 'https://worldcup26.ir' }}
          SCORE_SOURCE: ${{ vars.SCORE_SOURCE || 'fifa' }}
        run: node scripts/update-scores.mjs
      - id: commit
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          COMMENTARY_MODEL: ${{ vars.COMMENTARY_MODEL || 'claude-sonnet-4-6' }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if git diff --quiet public/games.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            # Scores changed → only now do we (maybe) call the AI. The script
            # itself skips the API unless a match just finished.
            node scripts/generate-commentary.mjs
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add public/games.json public/commentary.json
            git commit -m "Update scores and commentary"
            git push
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

  deploy:
    if: needs.update.outputs.changed == 'true'
    needs: update
    permissions:
      contents: read
      id-token: write
      pages: write
    uses: ./.github/workflows/deploy.yml

# Registered via touch-push: GitHub skipped indexing this schedule-only workflow on first push
